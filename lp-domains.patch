diff --git a/src/components/lp/landing-view.tsx b/src/components/lp/landing-view.tsx
index 7b6f919..a33332b 100644
--- a/src/components/lp/landing-view.tsx
+++ b/src/components/lp/landing-view.tsx
@@ -18,6 +18,20 @@ function FormBlock({
   if (page.embed) {
     return <OrderOnlineForm embed={page.embed} />;
   }
+  if (page.checkoutUrl) {
+    return (
+      <div id="pesan" className="scroll-mt-3 px-4 py-2">
+        <a
+          href={page.checkoutUrl}
+          rel="noopener noreferrer"
+          onClick={() => trackLpCheckout()}
+          className="flex h-12 w-full items-center justify-center rounded-full bg-[#e11d2e] text-[15px] font-bold text-white uppercase"
+        >
+          Isi form pemesanan
+        </a>
+      </div>
+    );
+  }
   return (
     <div
       id="pesan"
@@ -64,22 +78,33 @@ export function LandingView({
         productName={page.productName}
         price={page.price}
       />
-      <div className={`lp w-full max-w-md bg-white ${page.embed ? "pb-24" : ""}`}>
-        <LpCreative page={page} form={<FormBlock page={page} />} />
-        {page.embed ? (
+      <div className={`lp w-full max-w-md bg-white ${page.embed || page.checkoutUrl ? "pb-24" : ""}`}>
+        <LpCreative page={page} form={page.checkoutUrl ? null : <FormBlock page={page} />} />
+        {page.embed || page.checkoutUrl ? (
           <div className="fixed inset-x-0 bottom-0 z-40 border-t border-black/10 bg-white/95 px-4 py-3 pb-[max(0.75rem,env(safe-area-inset-bottom))] backdrop-blur-sm">
             <div className="mx-auto flex max-w-md items-center gap-3">
               <div className="min-w-0 flex-1">
                 <p className="truncate text-sm text-[#666]">{page.productName}</p>
                 <p className="font-bold tabular-nums text-[#e11d2e]">{rupiah(page.price)}</p>
               </div>
-              <button
-                type="button"
-                onClick={scrollToForm}
-                className="h-12 shrink-0 rounded-full bg-[#e11d2e] px-5 text-[15px] font-bold text-white uppercase"
-              >
-                {page.ctaPrimary || "PESAN"}
-              </button>
+              {page.checkoutUrl ? (
+                <a
+                  href={page.checkoutUrl}
+                  rel="noopener noreferrer"
+                  onClick={() => trackLpCheckout()}
+                  className="grid h-12 shrink-0 place-items-center rounded-full bg-[#e11d2e] px-5 text-[15px] font-bold text-white uppercase"
+                >
+                  {page.ctaPrimary || "PESAN"}
+                </a>
+              ) : (
+                <button
+                  type="button"
+                  onClick={scrollToForm}
+                  className="h-12 shrink-0 rounded-full bg-[#e11d2e] px-5 text-[15px] font-bold text-white uppercase"
+                >
+                  {page.ctaPrimary || "PESAN"}
+                </button>
+              )}
             </div>
           </div>
         ) : null}
diff --git a/src/components/lp/lp-creative.tsx b/src/components/lp/lp-creative.tsx
index 616e50d..f363a31 100644
--- a/src/components/lp/lp-creative.tsx
+++ b/src/components/lp/lp-creative.tsx
@@ -38,16 +38,38 @@ function scrollToForm() {
   scrollToLpForm();
 }
 
-function Cta({ label, pulse }: { label: string; pulse?: boolean }) {
-  return (
-    <button
-      type="button"
-      onClick={scrollToForm}
-      className={`flex h-12 w-full items-center justify-center gap-2 rounded-full bg-[#e11d2e] text-[15px] font-bold tracking-wide text-white uppercase ${pulse ? "cta-pulse" : ""}`}
-    >
+function Cta({
+  label,
+  pulse,
+  href,
+}: {
+  label: string;
+  pulse?: boolean;
+  href?: string;
+}) {
+  const className = `flex h-12 w-full items-center justify-center gap-2 rounded-full bg-[#e11d2e] text-[15px] font-bold tracking-wide text-white uppercase ${pulse ? "cta-pulse" : ""}`;
+  const inner = (
+    <>
       <ShoppingCart className="size-5" />
       {label}
       <span className="text-lg leading-none">›</span>
+    </>
+  );
+  if (href) {
+    return (
+      <a
+        href={href}
+        className={className}
+        rel="noopener noreferrer"
+        onClick={() => trackLpCheckout()}
+      >
+        {inner}
+      </a>
+    );
+  }
+  return (
+    <button type="button" onClick={scrollToForm} className={className}>
+      {inner}
     </button>
   );
 }
@@ -139,14 +161,16 @@ export function LpCreative({
         </p>
 
         {hero ? (
-          <Photo
-            src={hero}
-            priority
-            width={640}
-            height={640}
-            sizes="(max-width: 28rem) 100vw, 448px"
-            className="mx-auto mt-3 max-h-72 w-auto object-contain"
-          />
+          <div className="-mx-4 mt-3 bg-white">
+            <Photo
+              src={hero}
+              priority
+              width={720}
+              height={900}
+              sizes="(max-width: 28rem) 100vw, 448px"
+              className="block h-auto w-full object-contain"
+            />
+          </div>
         ) : null}
 
         <h1 className="mt-4 text-center text-[26px] leading-[1.15] font-black tracking-tight text-balance uppercase">
@@ -174,22 +198,22 @@ export function LpCreative({
         </ul>
 
         <div className="mt-4">
-          <Cta label={cta1} />
+          <Cta label={cta1} href={page.checkoutUrl} />
         </div>
         {form ? <div className="mt-5">{form}</div> : null}
 
         {thumbs.length ? (
           <div className="mt-4 grid grid-cols-2 gap-2">
             {thumbs.map((src, i) => (
-              <figure key={src} className="overflow-hidden rounded-md bg-[#111]">
+              <figure key={src} className="overflow-hidden bg-white">
                 <Photo
                   src={src}
-                  width={320}
-                  height={220}
+                  width={360}
+                  height={360}
                   sizes="(max-width: 28rem) 50vw, 200px"
-                  className="aspect-[4/3] w-full object-cover"
+                  className="aspect-square w-full object-contain"
                 />
-                <figcaption className="px-2 py-1.5 text-center text-[11px] font-semibold text-white">
+                <figcaption className="px-2 py-1.5 text-center text-[11px] font-semibold text-[#111]">
                   {i === 0 ? "Masalahnya" : "Pakai rutin"}
                 </figcaption>
               </figure>
@@ -334,7 +358,7 @@ export function LpCreative({
           Jangan cuma lihat-lihat. Selagi ready, langsung checkout.
         </p>
         <div className="mt-4">
-          <Cta label={cta2} pulse />
+          <Cta label={cta2} pulse href={page.checkoutUrl} />
         </div>
         <ul className="mt-4 flex flex-wrap justify-center gap-x-4 gap-y-1 text-[11px] text-white/75">
           <li className="inline-flex items-center gap-1">
diff --git a/src/lib/lp-fn.ts b/src/lib/lp-fn.ts
index 2e38cfd..12d9828 100644
--- a/src/lib/lp-fn.ts
+++ b/src/lib/lp-fn.ts
@@ -17,11 +17,13 @@ import {
   type OrderStatus,
   type ProductOption,
 } from "@/lib/lp";
-import { parseOrderOnlineEmbed } from "@/lib/orderonline";
+import { parseLpCheckout, parseOrderOnlineEmbed } from "@/lib/orderonline";
 import { grokExtremeCopy } from "@/lib/lp-ai";
 import { parseMetaPixelId, parseTiktokPixelId } from "@/lib/lp-pixels";
 import { resolveCdnBase } from "@/lib/settings-fn";
 import { normalizeLpHost } from "@/lib/lp-domains";
+import { decryptSecret } from "@/lib/secret";
+import { resolveXaiKey, setXaiSettingsKey } from "@/lib/xai";
 
 type LandingRow = {
   id: number;
@@ -100,6 +102,7 @@ function flag(value: unknown): boolean {
 }
 
 function mapLanding(r: LandingRow): LandingPage {
+  const checkout = parseLpCheckout(r.embed_code ?? "");
   return {
     id: r.id,
     productId: r.product_id,
@@ -126,7 +129,8 @@ function mapLanding(r: LandingRow): LandingPage {
     formTitle: r.form_title,
     formNote: r.form_note,
     embedCode: r.embed_code ?? "",
-    embed: parseOrderOnlineEmbed(r.embed_code ?? ""),
+    embed: checkout.embed,
+    checkoutUrl: checkout.checkoutUrl,
     metaPixelId: r.meta_pixel_id ?? "",
     tiktokPixelId: r.tiktok_pixel_id ?? "",
     publicDomain: r.public_domain ?? "",
@@ -496,7 +500,17 @@ export const createLanding = createServerFn({ method: "POST" })
     const cost = parseMoney(product.cost_price);
     const fallback = defaultLandingCopy(product.name, cost);
     const first = photos[0];
-    const ai = first?.data
+    if (!resolveXaiKey()) {
+      const stored = await sql.query<{ value: string }>(
+        `select value from app_settings where key = 'xai_key'`,
+      );
+      setXaiSettingsKey(decryptSecret(stored[0]?.value ?? ""));
+    }
+    let aiError = "";
+    if (!resolveXaiKey()) {
+      aiError = "Kunci AI belum diisi. Buka Pengaturan → Kunci AI.";
+    }
+    const ai = !aiError && first?.data
       ? await grokExtremeCopy({
           name: product.name,
           sku: product.sku,
@@ -504,6 +518,9 @@ export const createLanding = createServerFn({ method: "POST" })
           data: first.data,
         })
       : null;
+    if (!aiError && !ai) {
+      aiError = "Grok gagal baca foto. Cek log journalctl -u kaliaren | grep xai";
+    }
     const copy = ai ? { ...fallback, ...ai } : fallback;
     const source = ai ? ("grok" as const) : ("template" as const);
 
@@ -558,6 +575,7 @@ export const createLanding = createServerFn({ method: "POST" })
         slug: existing[0].slug,
         reused: true as const,
         source,
+        aiError,
       };
     }
 
@@ -598,7 +616,7 @@ export const createLanding = createServerFn({ method: "POST" })
     );
     const id = inserted[0]?.id;
     if (!id) throw new Error("Gagal membuat landing page");
-    return { id, slug, reused: false as const, source };
+    return { id, slug, reused: false as const, source, aiError };
   });
 
 export const updateLanding = createServerFn({ method: "POST" })
@@ -607,10 +625,13 @@ export const updateLanding = createServerFn({ method: "POST" })
   .handler(async ({ data, context }) => {
     await assertActiveStaff(context.userId);
     const embedCode = data.embedCode.trim();
-    if (embedCode && !parseOrderOnlineEmbed(embedCode)) {
-      throw new Error(
-        "Kode embed OrderOnline tidak lengkap. Tempel kode utuh termasuk script.",
-      );
+    if (embedCode) {
+      const parsed = parseLpCheckout(embedCode);
+      if (!parsed.embed && !parsed.checkoutUrl) {
+        throw new Error(
+          "Tempel embed OrderOnline, atau tautan checkout (https://...).",
+        );
+      }
     }
     const metaPixelId = parseMetaPixelId(data.metaPixelId);
     if (data.metaPixelId.trim() && !metaPixelId) {
diff --git a/src/lib/lp.ts b/src/lib/lp.ts
index bb1fe94..99f8fde 100644
--- a/src/lib/lp.ts
+++ b/src/lib/lp.ts
@@ -55,6 +55,7 @@ export type LandingPage = {
   formNote: string;
   embedCode: string;
   embed: OrderOnlineEmbed | null;
+  checkoutUrl?: string;
   metaPixelId: string;
   tiktokPixelId: string;
   publicDomain: string;
diff --git a/src/lib/orderonline.ts b/src/lib/orderonline.ts
index 7f6876c..68cca5b 100644
--- a/src/lib/orderonline.ts
+++ b/src/lib/orderonline.ts
@@ -1,5 +1,5 @@
-export const OO_JQUERY_SRC = "https://cdn.orderonline.id/js/vendor/jquery.min.js";
 export const OO_EMBED_SRC = "https://cdn.orderonline.id/js/embed-v2-slim.min.js?v=8.0.2";
+export const OO_JQUERY_SRC = "https://cdn.orderonline.id/js/vendor/jquery.min.js";
 
 export type OrderOnlineEmbed = {
   username: string;
@@ -81,3 +81,30 @@ export function orderOnlineBootScript(embed: OrderOnlineEmbed): string {
   const q = JSON.stringify;
   return `(function(){if(window.__ooBoot)return;window.__ooBoot=1;function n(){var o=window.ooe;if(o)return o;function e(){if(e.callMethod)e.callMethod.apply(e,arguments);else e.queue.push(arguments)}e.queue=[];e.loaded=true;e.version="8.0.2";e.push=e;window.ooe=e;window._ooe=e;return e}var o=n();o("setup","redirect",${q(embed.redirect)});o("init",${q(embed.storeId)},${q(embed.productId)},null,${q(embed.formId)},${JSON.stringify({ mode: embed.mode, action: embed.action, title: embed.title, triggerPixel: false, triggerGtm: false })});function add(src,id,cb){if(document.getElementById(id)){cb();return}var s=document.createElement("script");s.id=id;s.async=true;s.src=src;s.onload=cb;s.onerror=cb;document.head.appendChild(s)}add(${q(OO_JQUERY_SRC)},"oo-embed-jquery",function(){add(${q(OO_EMBED_SRC)},"oo-embed-js",function(){})})})();`;
 }
+
+export function parseCheckoutUrl(raw: string): string {
+  const s = raw.trim();
+  if (!s || /orderonline-embed-form|ooe\s*\(/i.test(s)) return "";
+  const m =
+    s.match(/https?:\/\/[^\s"'<>]+/i) ||
+    s.match(/^(?:www\.)?[a-z0-9.-]+\.[a-z]{2,}(?:\/\S*)?$/i);
+  if (!m) return "";
+  const href = m[0].startsWith("http") ? m[0] : `https://${m[0]}`;
+  try {
+    const u = new URL(href);
+    if (u.protocol !== "http:" && u.protocol !== "https:") return "";
+    if (!u.hostname || u.hostname === "localhost") return "";
+    return u.toString();
+  } catch {
+    return "";
+  }
+}
+
+export function parseLpCheckout(raw: string): {
+  embed: OrderOnlineEmbed | null;
+  checkoutUrl: string;
+} {
+  const embed = parseOrderOnlineEmbed(raw);
+  if (embed) return { embed, checkoutUrl: "" };
+  return { embed: null, checkoutUrl: parseCheckoutUrl(raw) };
+}
diff --git a/src/lib/settings-fn.ts b/src/lib/settings-fn.ts
index 4d81a53..a0a2770 100644
--- a/src/lib/settings-fn.ts
+++ b/src/lib/settings-fn.ts
@@ -15,6 +15,7 @@ import {
 import { assertActiveStaff } from "@/lib/staff-fn";
 import { decryptSecret, encryptSecret } from "@/lib/secret";
 import { cpanelAddDomain, cpanelDeleteDomain, cpanelListDomains } from "@/lib/cpanel";
+import { setXaiSettingsKey } from "@/lib/xai";
 import { execFile } from "node:child_process";
 import { promisify } from "node:util";
 
@@ -364,3 +365,27 @@ export const removeLpDomain = createServerFn({ method: "POST" })
     };
   });
 
+export const getAiConfig = createServerFn({ method: "GET" })
+  .middleware([authMiddleware])
+  .handler(async ({ context }) => {
+    const profile = await assertActiveStaff(context.userId);
+    if (profile.role !== "admin") throw new Error("Hanya admin");
+    const env = Boolean(process.env.XAI_API_KEY?.trim());
+    const stored = Boolean(decryptSecret(await readSetting("xai_key")));
+    return { hasKey: env || stored, fromEnv: env };
+  });
+
+export const saveAiKey = createServerFn({ method: "POST" })
+  .middleware([authMiddleware])
+  .validator(z.object({ key: z.string().max(200) }))
+  .handler(async ({ data, context }) => {
+    const profile = await assertActiveStaff(context.userId);
+    if (profile.role !== "admin") throw new Error("Hanya admin");
+    const key = data.key.trim();
+    if (key) {
+      await writeSetting("xai_key", encryptSecret(key));
+      setXaiSettingsKey(key);
+    }
+    return { ok: true as const, hasKey: Boolean(key || process.env.XAI_API_KEY) };
+  });
+
diff --git a/src/lib/xai.ts b/src/lib/xai.ts
index a6ef934..cb9fdc9 100644
--- a/src/lib/xai.ts
+++ b/src/lib/xai.ts
@@ -8,6 +8,16 @@ export function xaiKey(): string | undefined {
   return key || undefined;
 }
 
+let settingsKey = "";
+
+export function setXaiSettingsKey(key: string) {
+  settingsKey = key.trim();
+}
+
+export function resolveXaiKey(): string | undefined {
+  return xaiKey() || settingsKey || undefined;
+}
+
 type ChatContent =
   | string
   | Array<
@@ -21,7 +31,7 @@ export async function grokChat(opts: {
   maxTokens?: number;
   timeoutMs?: number;
 }): Promise<string | null> {
-  const apiKey = xaiKey();
+  const apiKey = resolveXaiKey();
   if (!apiKey) {
     console.error("[xai] no key");
     return null;
@@ -37,39 +47,45 @@ export async function grokChat(opts: {
       ]
     : opts.prompt;
 
+  const models = ["grok-4-fast-reasoning", "grok-4.5"];
   const ctrl = new AbortController();
   const timer = setTimeout(() => ctrl.abort(), opts.timeoutMs ?? 60_000);
   try {
-    const res = await fetch(XAI_CHAT_URL, {
-      method: "POST",
-      signal: ctrl.signal,
-      headers: {
-        "Content-Type": "application/json",
-        Authorization: `Bearer ${apiKey}`,
-      },
-      body: JSON.stringify({
-        model: "grok-4.5",
-        max_tokens: opts.maxTokens ?? 1800,
-        temperature: 0.6,
-        reasoning_effort: "low",
-        response_format: { type: "json_object" },
-        messages: [{ role: "user", content }],
-      }),
-    });
-    if (!res.ok) {
-      const errBody = await res.text().catch(() => "");
-      console.error("[xai]", res.status, errBody.slice(0, 240));
-      return null;
-    }
-    const body = (await res.json()) as {
-      choices?: { message?: { content?: string }; finish_reason?: string }[];
-    };
-    const text = body.choices?.[0]?.message?.content?.trim() || "";
-    if (!text) {
-      console.error("[xai] empty", body.choices?.[0]?.finish_reason);
-      return null;
+    let last = "";
+    for (const model of models) {
+      const res = await fetch(XAI_CHAT_URL, {
+        method: "POST",
+        signal: ctrl.signal,
+        headers: {
+          "Content-Type": "application/json",
+          Authorization: `Bearer ${apiKey}`,
+        },
+        body: JSON.stringify({
+          model,
+          max_tokens: opts.maxTokens ?? 1800,
+          temperature: 0.6,
+          reasoning_effort: "low",
+          response_format: { type: "json_object" },
+          messages: [{ role: "user", content }],
+        }),
+      });
+      if (!res.ok) {
+        last = `${model} ${res.status} ${(await res.text().catch(() => "")).slice(0, 180)}`;
+        console.error("[xai]", last);
+        continue;
+      }
+      const body = (await res.json()) as {
+        choices?: { message?: { content?: string }; finish_reason?: string }[];
+      };
+      const text = body.choices?.[0]?.message?.content?.trim() || "";
+      if (!text) {
+        last = `${model} empty ${body.choices?.[0]?.finish_reason ?? ""}`;
+        console.error("[xai]", last);
+        continue;
+      }
+      return text;
     }
-    return text;
+    return null;
   } catch (err) {
     console.error("[xai]", err instanceof Error ? err.name : "fail");
     return null;
@@ -83,7 +99,7 @@ export async function grokImagineEdit(opts: {
   imageDataUrl: string;
   timeoutMs?: number;
 }): Promise<{ mime: string; data: string } | null> {
-  const apiKey = xaiKey();
+  const apiKey = resolveXaiKey();
   if (!apiKey) return null;
   const ctrl = new AbortController();
   const timer = setTimeout(() => ctrl.abort(), opts.timeoutMs ?? 90_000);
diff --git a/src/routes/lp.$id.tsx b/src/routes/lp.$id.tsx
index ef73c54..f33beba 100644
--- a/src/routes/lp.$id.tsx
+++ b/src/routes/lp.$id.tsx
@@ -8,7 +8,7 @@ import { listLpDomains } from "@/lib/settings-fn";
 import type { LandingPage } from "@/lib/lp";
 import { discountPercent } from "@/lib/lp";
 import { lpAdUrl, mergeDomainChoices, normalizeLpHost } from "@/lib/lp-domains";
-import { describeEmbed, parseOrderOnlineEmbed } from "@/lib/orderonline";
+import { describeEmbed, parseLpCheckout } from "@/lib/orderonline";
 import { downloadNodeAsWebp } from "@/lib/export-webp";
 import { LpCreative } from "@/components/lp/lp-creative";
 import type { Warehouse } from "@/lib/inventory";
@@ -245,8 +245,8 @@ function LandingEditorPage() {
     setExporting(true);
   }
 
-  const parsedEmbed = useMemo(
-    () => parseOrderOnlineEmbed(draft?.embedCode ?? ""),
+  const parsedCheckout = useMemo(
+    () => parseLpCheckout(draft?.embedCode ?? ""),
     [draft?.embedCode],
   );
 
@@ -378,33 +378,37 @@ function LandingEditorPage() {
       </fieldset>
 
       <fieldset className="grid gap-3 rounded-xl bg-surface p-4 shadow-[var(--shadow-border)]">
-        <legend className="px-1 text-sm font-semibold">Form OrderOnline</legend>
+        <legend className="px-1 text-sm font-semibold">Form / checkout</legend>
         <p className="text-sm text-muted">
-          Tempel kode embed dari OrderOnline untuk produk ini. Tiap LP form-nya
-          sendiri.
+          Tempel embed OrderOnline, atau tautan checkout. Domain iklan sering
+          gagal embed — tautan lebih aman. Tombol PESAN mengarah ke tautan itu.
         </p>
-        <Label htmlFor="embed-code">Kode embed</Label>
+        <Label htmlFor="embed-code">Embed atau tautan</Label>
         <Textarea
           id="embed-code"
-          className="min-h-40 font-mono text-xs"
+          className="min-h-28 font-mono text-xs"
           value={draft.embedCode}
           onChange={(e) => set("embedCode", e.target.value)}
-          placeholder='<!-- OrderOnline Embed Code --> …'
+          placeholder={"https://ahtashop.orderonline.id/produk-kamu\natau kode embed OrderOnline"}
           spellCheck={false}
         />
         {draft.embedCode.trim() ? (
-          parsedEmbed ? (
+          parsedCheckout.embed ? (
             <p className="text-sm text-ok">
-              Siap: {describeEmbed(parsedEmbed)}
+              Embed siap: {describeEmbed(parsedCheckout.embed)}
+            </p>
+          ) : parsedCheckout.checkoutUrl ? (
+            <p className="text-sm text-ok">
+              Tombol PESAN → {parsedCheckout.checkoutUrl}
             </p>
           ) : (
             <p className="text-sm text-danger">
-              Kode belum lengkap. Tempel utuh, termasuk bagian script.
+              Bukan embed utuh dan bukan tautan https://
             </p>
           )
         ) : (
           <p className="text-xs text-muted">
-            Kosong = LP tanpa form. Generate foto dulu, lalu tempel form di sini.
+            Kosong = LP tanpa checkout. Tempel tautan OrderOnline paling simpel.
           </p>
         )}
       </fieldset>
diff --git a/src/routes/pengaturan.tsx b/src/routes/pengaturan.tsx
index 91d28c0..57fda0c 100644
--- a/src/routes/pengaturan.tsx
+++ b/src/routes/pengaturan.tsx
@@ -11,10 +11,12 @@ import {
 import {
   connectLpDomain,
   getCpanelConfig,
+  getAiConfig,
   listLpDomains,
   refreshLpDomain,
   removeLpDomain,
   saveCpanelConfig,
+  saveAiKey,
   testCpanel,
 } from "@/lib/settings-fn";
 import { LP_A_RECORD_IP, type LpDomain } from "@/lib/lp-domains";
@@ -41,6 +43,8 @@ function SettingsPage() {
   const [cpanelOk, setCpanelOk] = useState<boolean | null>(null);
   const [cpanelDomains, setCpanelDomains] = useState<string[]>([]);
   const [cpanelError, setCpanelError] = useState("");
+  const [aiKey, setAiKey] = useState("");
+  const [hasAiKey, setHasAiKey] = useState(false);
 
   function applyList(d: { primary: string; extra: LpDomain[] }) {
     setPrimary(d.primary);
@@ -48,12 +52,13 @@ function SettingsPage() {
   }
 
   useEffect(() => {
-    Promise.all([listLpDomains(), getCpanelConfig()])
-      .then(([domains, cfg]) => {
+    Promise.all([listLpDomains(), getCpanelConfig(), getAiConfig()])
+      .then(([domains, cfg, ai]) => {
         applyList(domains);
         setHost(cfg.host);
         setUser(cfg.user);
         setHasToken(cfg.hasToken);
+        setHasAiKey(ai.hasKey);
       })
       .catch((e: Error) => toast.error(e.message));
   }, []);
@@ -312,6 +317,46 @@ function SettingsPage() {
               ) : null}
             </CardContent>
           </Card>
+
+          <Card>
+            <CardContent className="grid gap-3 p-5">
+              <div>
+                <p className="font-semibold">Kunci AI (Grok)</p>
+                <p className="text-sm text-muted">
+                  Untuk Generate LP dari foto. Tanpa kunci, LP hanya template.
+                </p>
+              </div>
+              <form
+                onSubmit={(e) => {
+                  e.preventDefault();
+                  if (!aiKey.trim()) return;
+                  setBusy(true);
+                  saveAiKey({ data: { key: aiKey } })
+                    .then((r) => {
+                      setHasAiKey(r.hasKey);
+                      setAiKey("");
+                      toast.success("Kunci AI disimpan");
+                    })
+                    .catch((err: Error) => toast.error(err.message))
+                    .finally(() => setBusy(false));
+                }}
+                className="grid gap-2"
+              >
+                <Input
+                  type="password"
+                  value={aiKey}
+                  onChange={(e) => setAiKey(e.target.value)}
+                  placeholder={hasAiKey ? "Tersimpan · isi untuk ganti" : "xai-..."}
+                />
+                <Button type="submit" disabled={busy || !aiKey.trim()}>
+                  Simpan kunci AI
+                </Button>
+              </form>
+              <p className={hasAiKey ? "text-xs text-ok" : "text-xs text-danger"}>
+                {hasAiKey ? "AI siap" : "AI belum aktif"}
+              </p>
+            </CardContent>
+          </Card>
         </div>
       </div>
 
diff --git a/src/routes/stok.$id.tsx b/src/routes/stok.$id.tsx
index aad1c8a..8d8c694 100644
--- a/src/routes/stok.$id.tsx
+++ b/src/routes/stok.$id.tsx
@@ -89,11 +89,11 @@ function ProductDetailPage() {
         data: { productId: product.id, warehouseId },
       });
       setLanding({ id: created.id, slug: created.slug, isPublished: true });
-      toast.success(
-        created.source === "grok"
-          ? "Grok baca foto. LP siap. Tempel form."
-          : "LP jadi. Tempel form.",
-      );
+      if (created.source === "grok") {
+        toast.success("Grok baca foto. LP siap. Tempel form.");
+      } else {
+        toast.error(created.aiError || "AI tidak jalan. LP pakai template.");
+      }
       navigate({ to: "/lp/$id", params: { id: String(created.id) } });
     } catch (err) {
       toast.error(err instanceof Error ? err.message : "Gagal generate LP");
